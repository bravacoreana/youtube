extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        each video in videos
            +videoBlock({
                id: video.id,
                title: video.title,
                thumbnail: video.thumbnailFile,
                views: video.views
            })